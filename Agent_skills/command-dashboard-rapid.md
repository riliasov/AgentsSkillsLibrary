# Command: Dashboard Rapid Development

> **Назначение:** Создание дашбордов с архитектурой "MVP → Production". Быстрый старт с возможностью последующей миграции в веб/BI.
>
> **Триггер:** Запросы на дашборд, аналитику, визуализацию данных.
>
> **Философия:** Быстрый MVP → Апгрейд до веб-приложения → Перенос в BI. Логика отделена от UI для простой миграции.

---

## Стратегия развития

### Путь миграции (3 этапа)

```
1. MVP / Прототип (Streamlit)
   → Цель: Быстрая валидация идеи, эксперименты
   → Время: 15-30 минут
   → Для: Себя, команда
   
2. Internal Dashboard (Dash)
   → Цель: Больше функций, production-ready
   → Время: 2-4 часа миграции
   → Для: Клиенты, стейкхолдеры
   → Фичи: Sticky headers, DataTable, сложные callbacks
   
3. Production Web (FastAPI + React)
   → Цель: Полноценный продукт
   → Время: 1-2 недели
   → Для: Публичное использование, интеграция в систему
   → Фичи: Полный контроль UI/UX, масштабируемость
```

### Ключевой принцип

**Разделение ответственности:**
- **Логика** (получение данных, расчеты) → отдельные модули
- **UI** (Streamlit/Dash/React) → только обертка для отображения

Это позволяет менять UI без переписывания бизнес-логики.

---

## Technology Stack

### 📋 Выбор стека зависит от этапа

**Этап 1: MVP (по умолчанию)**
- **UI:** Streamlit
- **Визуализация:** Plotly
- **Данные:** Pandas
- **БД:** SQLite / PostgreSQL / CSV (универсально)

**Этап 2: Internal Dashboard**
- **UI:** Dash
- **Визуализация:** Plotly
- **Данные:** Pandas
- **БД:** PostgreSQL / Supabase (рекомендуется)

**Этап 3: Production Web**
- **Backend:** FastAPI
- **Frontend:** React + Plotly.js
- **БД:** PostgreSQL
- **Deploy:** Docker + Nginx

### 🤖 Выбор AI-агента

**Если пользователь НЕ указал стек явно:**
1. Спроси: "Это MVP/прототип или сразу production?"
2. **Если MVP** → используй Streamlit (быстро)
3. **Если production** → предложи Dash или FastAPI+React
4. **Если не уверен** → начни со Streamlit, объясни путь миграции

---

## Архитектура проекта

### Принцип: Разделение ответственности

```
project_root/
├── config/
│   ├── __init__.py
│   ├── settings.py              # Настройки проекта (DB, API keys)
│   └── dashboard_config.py      # UI константы (цвета, метрики из skill)
│
├── data/
│   ├── __init__.py
│   ├── connectors.py            # Подключения к БД (SQLite, PostgreSQL, API)
│   └── queries.py               # SQL запросы / data fetching
│
├── business_logic/
│   ├── __init__.py
│   ├── metrics.py               # Расчет метрик (БЕЗ привязки к UI)
│   ├── transformations.py       # Обработка данных (фильтры, агрегации)
│   └── validators.py            # Validations, thresholds
│
├── ui/
│   ├── streamlit_app.py         # Streamlit версия (MVP)
│   ├── dash_app.py              # Dash версия (Internal)
│   └── components/              # Переиспользуемые UI компоненты
│       ├── charts.py            # Генерация Plotly графиков
│       ├── tables.py            # DataFrames, DataTables
│       └── filters.py           # Виджеты фильтров
│
├── run_streamlit.py             # Entry: запуск Streamlit
├── run_dash.py                  # Entry: запуск Dash
└── requirements.txt
```

### Почему так?

**1. `data/` — Доступ к данным**
- Содержит ТОЛЬКО логику получения данных
- БЕЗ обработки, БЕЗ UI
- Переиспользуется во всех UI версиях

**2. `business_logic/` — Бизнес-правила**
- Расчет метрик, фильтрация, агрегация
- Чистый Python, БЕЗ Streamlit/Dash зависимостей
- Тестируемо (unit tests)

**3. `ui/` — Только отображение**
- Импортирует `data/` и `business_logic/`
- Содержит ТОЛЬКО UI код
- Легко заменить (Streamlit → Dash → React)

---

## Workflow создания дашборда

### Этап 1: Сбор требований

**AI-агент спрашивает:**
1. **Этап проекта:** MVP, Internal, или Production?
2. **Источник данных:** SQLite/PostgreSQL/CSV/API?
3. **Ключевые метрики:** Что нужно визуализировать?
4. **Требования к UI:** Sidebar, фильтры, табы, таблицы с сортировкой, sticky headers?

### Этап 2: Создание структуры

**Автоматически:**
1. Создать папки: `config/`, `data/`, `business_logic/`, `ui/`
2. Сгенерировать `config/dashboard_config.py` из `skill-dashboard-design.md`
3. Создать заглушки модулей

**Принцип:** Начинай с минимального набора файлов, расширяй по необходимости.

### Этап 3: Реализация слоев

**3.1. Data Layer (`data/`)**
```python
# data/connectors.py
def get_db_connection(db_type='sqlite', **kwargs):
    """Универсальное подключение к БД."""
    pass

# data/queries.py  
def fetch_messages(start_date=None, end_date=None):
    """Получение данных без обработки."""
    return pd.DataFrame(...)
```

**3.2. Business Logic Layer (`business_logic/`)**
```python
# business_logic/metrics.py
def calculate_total_messages(df):
    """Расчет метрик БЕЗ привязки к UI."""
    return df['messages'].sum()

# business_logic/transformations.py
def filter_by_date_range(df, start, end):
    """Фильтрация данных."""
    return df[(df['date'] >= start) & (df['date'] <= end)]
```

**3.3. UI Layer (`ui/`)**
```python
# ui/streamlit_app.py (MVP)
import streamlit as st
from data.queries import fetch_messages
from business_logic.metrics import calculate_total_messages

df = fetch_messages()
total = calculate_total_messages(df)
st.metric("Total Messages", total)
```

### Этап 4: Генерация графиков

**Принцип:** Графики — это функции, которые возвращают Plotly figures.

```python
# ui/components/charts.py
def create_timeline_chart(df, color='#059669'):
    """Генерация линейного графика."""
    fig = go.Figure(...)
    return fig
```

Вызов в UI:
```python
# Streamlit
fig = create_timeline_chart(df)
st.plotly_chart(fig)

# Dash
fig = create_timeline_chart(df)
dcc.Graph(figure=fig)
```

**Результат:** Один и тот же график работает в Streamlit, Dash, и FastAPI+React.

---

## Миграция между стеками

### Streamlit → Dash

**Что переписывать:**
- ✅ `ui/streamlit_app.py` → `ui/dash_app.py`
- ❌ `data/`, `business_logic/` — **БЕЗ изменений**

**Примерное время:** 2-4 часа (только UI)

### Dash → FastAPI + React

**Что переписывать:**
- ✅ UI полностью (React компоненты)
- ✅ API endpoints (FastAPI routes)
- ❌ `data/`, `business_logic/` — **БЕЗ изменений**

**Примерное время:** 1-2 недели (фронтенд + API)

---

## Tips для AI-агента

### Гибкость vs Контроль

**Что предлагать:**
- Разные варианты решения (если есть альтернативы)
- Обоснование выбора стека/подхода
- Путь миграции (если MVP → production)

**Что НЕ делать:**
- Навязывать единственный вариант без объяснения
- Игнорировать требования пользователя
- Создавать излишне сложную архитектуру для MVP

### Управление ожиданиями

**Всегда говори:**
- Сколько времени займет (15 минут для MVP, 2 часа для Dash)
- Какие ограничения есть у выбранного подхода
- Когда понадобится миграция

### Приоритеты

1. **Скорость MVP** — главный приоритет (15-30 минут)
2. **Чистая архитектура** — разделение data/logic/ui
3. **Легкая миграция** — возможность апгрейда
4. **Дизайн-система** — применение skill-dashboard-design.md

---

## Итог

**Что запомнить:**
- Начинай с Streamlit (быстрый MVP)
- Разделяй логику и UI (легкая миграция)
- Предлагай варианты, объясняй выбор
- Следуй пути: MVP → Internal → Production
